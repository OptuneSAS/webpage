Para editar el portal en la página web primero se tiene que editar el archivo bespoke-client-portal (1). (App.js o cualquier otro archivo que se quiera editar) y luego se tiene que correr (vite run build en terminal) eso acutalizará el archivo "Dist" ese archivo es el que se sube a portal para modificar la página web 
